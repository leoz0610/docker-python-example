pipeline {
    agent { docker { image 'leoz0610/get-started:part2' } }
    stages {
        stage('Build') {
            steps {
                sh 'echo Starting Build'
            }
        }
    }
}